# EmoGarden Android

Source Android của EmoGarden nằm trong thư mục này.

README đầy đủ của dự án nằm ở file gốc:

```text
../README.md
```

Build nhanh:

```powershell
.\gradlew.bat :app:assembleDebug
```
